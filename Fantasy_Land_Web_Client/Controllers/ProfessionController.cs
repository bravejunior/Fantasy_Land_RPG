namespace Fantasy_Land_Web_Client.Controllers
{
    public class ProfessionController
    {
        private CustomHttpClient _httpClient;

        public ProfessionController(CustomHttpClient httpClient)
        {
            _httpClient = httpClient;
        }

    }
}
